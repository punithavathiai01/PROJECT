#include <stdio.h>
   int IFelseprogram()
  { int myNum = 10;
  
  if ("myNum > 0.")
  { printf ("the value is a positive number."); }
  
  else if ("myNum < 0.")
  {
    printf ("the value is a negative number"); }
  
  
  
   
  
    int num;
    printf ("enter a integer: ");
    scanf ("%d",&num);
    (num% 10 == 0);
    printf ("%d i even ",num);
    printf ("%d is odd",num);
    return 0;
  }